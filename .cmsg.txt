Cities scanner + Portfolio→Memory bridge

City scanner (runtime/calendar/city_scanner.py — new):
- CityEventScanner: scan_city / get_landmarks / get_notable_dates per city
- 6 sources: Ticketmaster (latLong+classification), Eventbrite (BeautifulSoup→
  JSON-LD regex fallback), Reddit (per-city subreddit), Google Trends
  (pytrends optional), NewsAPI (key-gated), curated JSONL
- Module-level cache (1h TTL) + atomic JSONL persistence

Bootstrap data:
- runtime/calendar/data/landmarks.json — 10 landmarks per city x 7 cities
- runtime/calendar/data/notable_dates.json — 8-10 annual festivals/dates per
  city with multi-day support (end_month/end_day). K-Days, Stampede,
  Carnaval, Guelaguetza, Día de los Muertos, Noche de Rábanos, etc.
- data/calendar/curated_events/{7 cities}.jsonl — weekly recurring events
  (markets, open mics, ciclovías, candombe drums)

Integration:
- Awarebot.scan_cycle adds 7th source 'city_events'. Per-city 1h throttle
  prevents Ticketmaster/Eventbrite/Reddit hammering on the 5min Awarebot tick
- New scheduler loop ncl-city-events (1h cadence) → per-city refresh +
  enqueue to MemoryStore via async_writer w/ source=awarebot:city_events:{city}
- /calendar/local/{city} now returns rich payload: weather + events (21) +
  landmarks (10) + notable_dates (34) + fun_finder {today, this_week,
  this_month}. ?flat=true preserves legacy contract.

Portfolio→Memory bridge (runtime/portfolio/memory_bridge.py — new):
- PortfolioMemoryBridge diffs each sync, emits portfolio:* units to MemoryStore
  via AsyncMemoryWriter with authority_tier=NATRIX (100 — user's money is
  highest authority)
- Event-driven writes: position_opened/closed (importance 85), significant_move
  >5% day on position or >2% account (importance 80), bp_risk <5% NLV
  (importance 95), quantity_change (75)
- Hourly snapshot during market hours (semantic, importance 70)
- runtime/portfolio/portfolio_manager.py calls bridge.on_sync(summary,
  positions) after every successful sync (60s mkt / 5min off-hrs)
- Warm-starts from snapshots.jsonl on init
- runtime/memory/authority.py maps portfolio:* sources to NATRIX
- runtime/memory/working_context.py new _pull_portfolio_candidates always
  pulls freshest snapshot + last 5 events into daily window
- runtime/memory/chat_context.py _build_portfolio_section + _should_inject
  heuristic (matches $TICKER, finance hint words). Renders PORTFOLIO STATE
  block w/ NLV, day P&L, top 5 positions, last 3 significant events.
  Soft-trimmed at ~800 tokens. Counts against chat_injection budget cap.

New endpoints:
- GET /portfolio/events?limit=&source=
- GET /portfolio/significant-moves?days=&scope=
- GET /portfolio/bridge-state (verification)

Live verified post-restart:
- /calendar/local/edmonton: 21 events, 10 landmarks, 34 notable dates,
  fun_finder today=5/week=4/month=12 — sources: holidays/weather/
  ticketmaster/curated/reddit/notable_date
- /portfolio/bridge-state: bridge_initialized=true, latest_at=08:48:22 UTC,
  nlv=$20,628.97 CAD, position_count=12, top 5 positions cached

Caveats:
- Pre-existing MemoryStore reader-counter lock leak under Awarebot flood
  can wedge create_unit; bridge falls back to in-memory cache so chat
  injector reads work regardless. Worth fixing separately.
- TM_API_KEY + NEWSAPI_KEY unset in .env — those sources skip silently;
  add keys to unlock more event sources.
